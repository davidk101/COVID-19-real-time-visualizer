import React from 'react'
import ReactDOM from 'react-dom' // only in index.js
import App from './App'

ReactDOM.render(<App />,document.getElementById('root')) // render function loads the element <App/> into the DOM in root container.